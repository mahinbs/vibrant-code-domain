# SPA fallback for hosts that honor Netlify-style redirects (e.g. Netlify).
# Vercel uses vercel.json rewrites; this file is harmless there if ignored.
/for-llm    /for-llm/index.html    200
/*          /index.html            200
